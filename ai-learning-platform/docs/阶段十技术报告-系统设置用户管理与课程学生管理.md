# 阶段十技术报告：系统设置、用户管理扩展与课程学生管理

> 日期：2026-09-02
> 范围：AI 辅助在线学习平台（后端 + 管理端）
> 目标：接入 GLM-4.7-Flash 新模型与新密钥并增加 AI 总开关；补全管理端"系统设置"功能点；扩展用户管理（管理员对学生/教师的增删改查、重置密码）；新增课程学生管理（教师管理自己课程的学生、管理员可管理任意教师课程的学生）；完成全量回归测试后同步仓库。

---

## 一、任务背景与要点拆解

本阶段收到四项需求，拆解为如下开发要点：

| # | 要点 | 实现方式 |
|---|------|----------|
| 1 | AI 功能使用新密钥，模型切换为 `glm-4.7-flash` | 更新 `application.yml` 配置；保留环境变量覆盖能力 |
| 2 | 管理端系统设置功能点 | 新建 `system_config` 表 + `SystemConfigService` + 管理端设置页（站点名称 / AI 总开关 / 注册开关） |
| 3 | 用户管理扩展：admin 对学生/教师增删改查、修改密码 | `UserManageService` 增加 create/update/delete/resetPassword，删除时级联清理业务数据；管理端页面新增弹窗 |
| 4 | 课程学生管理：教师查看/管理自己课程报名学生；admin 可管理任意教师课程的学生 | 新建 `CourseStudentService` + `CourseStudentController`（按角色鉴权）；管理端新增教师端/管理员端共用页面 |

---

## 二、AI 模型切换与总开关

### 2.1 配置更新

`backend/src/main/resources/application.yml`：

```yaml
spring:
  ai:
    openai:
      api-key: ${ZHIPU_API_KEY:7bcfccd6e77e416789ae78ae4ddcec23.7OSmeF0NkcaMOqZS}
      base-url: ${AI_BASE_URL:https://open.bigmodel.cn/api/paas/v4}
      chat:
        options:
          model: ${AI_MODEL:glm-4.7-flash}
```

密钥与模型均支持环境变量覆盖，代码中不硬编码。

### 2.2 AI 功能总开关

`ZhipuAiClient` 注入 `SystemConfigService`，所有 AI 调用（流式答疑 `chatStream` / 非流式出题批改 `chat`）统一经过 `checkAvailable()` 校验：

```java
private void checkAvailable() {
    if (!systemConfigService.isEnabled("ai_enabled")) {
        throw new BizException("AI 功能已被管理员关闭");
    }
    if (!isConfigured()) {
        throw new BizException("AI 服务未配置，请联系管理员");
    }
}
```

管理员在系统设置页关闭总开关后，学生端答疑、教师端出题/批改均**不发起任何模型调用**，直接返回降级提示。

### 2.3 实测结论

- 直连智谱 API 与经平台出题接口实测均成功（生成判断题 2 道、单选 2 道）；
- `glm-4.7-flash` 为免费模型，高峰期存在间歇性 `429（code 1305 该模型当前访问量过大）`，客户端已内置**最多 2 次指数退避重试**，仍失败时提示"AI 服务限流，请稍后重试"，属外部服务容量问题，非集成缺陷。

---

## 三、管理端系统设置

### 3.1 数据库

新增 `system_config` 表（键值对 + 说明 + 更新时间，`config_key` 唯一）：

```sql
CREATE TABLE `system_config` (
  `id`           BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
  `config_key`   VARCHAR(50)  NOT NULL COMMENT '配置键',
  `config_value` VARCHAR(200) NOT NULL DEFAULT '' COMMENT '配置值',
  `remark`       VARCHAR(100) COMMENT '配置说明',
  `update_time`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE = InnoDB COMMENT = '系统设置表';
```

初始配置项：

| config_key | 说明 | 默认值 |
|-----------|------|--------|
| `site_name` | 站点名称 | AI 辅助在线学习平台 |
| `ai_enabled` | AI 功能总开关（1 开启 0 关闭） | 1 |
| `register_enabled` | 开放学生自主注册（1 开启 0 关闭） | 1 |

### 3.2 后端

- `SystemConfigService.list()` / `save(Map)`：仅管理员可操作（`UserContext.checkRole(ROLE_ADMIN)`）；保存时校验 key 白名单，逐项 upsert；
- `isEnabled(key)`：供各业务读取开关（默认开启，仅显式为 `0` 时关闭）；
- 接口：`GET /admin/ops/settings`、`POST /admin/ops/settings`（批量保存）。

### 3.3 注册开关联动

`AuthController.register` 在注册前检查 `register_enabled`，关闭时返回"平台已关闭自主注册，请联系管理员开通账号"——关闭自主注册后，账号统一由管理员在"用户管理"中创建。

### 3.4 管理端页面

`SystemSettingsView.vue`（路由 `/admin/settings`，菜单"系统设置"）：站点名称输入框 + 注册开关 + AI 总开关（带状态说明文案），保存后即时生效。

---

## 四、用户管理扩展

### 4.1 后端接口

`AdminOpsController` 新增：

| 接口 | 说明 |
|------|------|
| `POST /admin/ops/users` | 新增用户（指定学生/教师/管理员角色 + 初始密码） |
| `PUT /admin/ops/users/{userId}` | 编辑用户（昵称/角色/状态） |
| `DELETE /admin/ops/users/{userId}` | 删除用户（级联清理业务数据） |
| `POST /admin/ops/users/{userId}/reset-password` | 重置密码 |

### 4.2 业务规则

- **新增**：用户名唯一校验；密码 BCrypt 加密；角色限定学生(1)/教师(2)/管理员(3)；初始积分账户同步创建；
- **编辑**：用户名不可修改；角色可调整；
- **重置密码**：新密码 BCrypt 加密后覆盖，旧密码立即失效；
- **删除**：不允许删除自己（`admin` 删除自己会导致平台无管理员）；事务内级联清理该用户的 AI 会话与消息、选课、学习记录、笔记、考试答题/考试记录/练习记录、积分账户/流水/签到/活动/优惠券、课程评论，最后删除用户本体，避免孤儿数据。

### 4.3 管理端页面

`UserManageView.vue` 扩展：

- 工具栏新增"新增用户"按钮 → 弹窗（用户名 / 初始密码 / 昵称 / 角色）；
- 行操作扩展为：编辑（弹窗回填）、重置密码（弹窗二次确认输入）、禁用/启用、删除（二次确认）。

---

## 五、课程学生管理

### 5.1 权限模型

| 角色 | 可见课程 | 可执行操作 |
|------|---------|-----------|
| 教师 | 仅自己创建的课程 | 查看学生分页 / 添加学生 / 调整进度 / 移除学生 |
| 管理员 | 全部教师创建的课程 | 同教师（可跨教师操作任意课程） |
| 学生 | 无权限 | 接口直接 403 |

`checkCourseAccess()` 统一鉴权：教师访问他人课程返回 `403 只能管理自己创建的课程`。

### 5.2 后端接口

`CourseStudentController`（`/course/{courseId}/students`）：

| 接口 | 说明 |
|------|------|
| `GET ?page=&size=&keyword=` | 课程学生分页（含学生信息、账号状态、学习进度、选课时间），支持用户名/昵称关键字过滤 |
| `GET /candidates?keyword=` | 可添加学生候选（未选本课的启用状态学生，最多 50 条） |
| `POST` | 添加学生（按 `studentId` 或 `username`；仅学生角色；已在课幂等返回） |
| `PUT /{enrollmentId}` | 调整学习进度（0-100 校验） |
| `DELETE /{enrollmentId}` | 移除学生（删除选课记录，学习记录保留但不再计入课程） |

### 5.3 管理端页面

新建共用页面 `CourseStudentsView.vue`：

- **教师端**：菜单"教学管理 → 学生管理"（路由 `/teacher/course/students`），课程下拉仅显示自己的课程；
- **管理员端**：菜单"课程管理 → 学生管理"（路由 `/admin/course/students`），课程下拉显示全部课程，页面附权限说明提示条；
- 功能：课程选择 → 学生分页表格（头像/昵称/账号状态/进度条/选课时间）→ 添加学生弹窗（"从候选学生选择"支持搜索、"按用户名添加"双模式）→ 调整进度弹窗 → 移除二次确认。

---

## 六、回归测试

### 6.1 接口测试（全部通过）

| 模块 | 用例 | 结果 |
|------|------|------|
| 系统设置 | 管理员查询/保存配置；教师访问被拒（403） | 通过 |
| 注册开关 | 关闭后注册被拒（"平台已关闭自主注册"）；开启后注册成功 | 通过 |
| AI 总开关 | 关闭后 AI 出题被拒（"AI 功能已被管理员关闭"）；开启后真实调用 glm-4.7-flash 出题成功 | 通过 |
| 用户管理 | 新增学生/教师；新用户登录；编辑（改昵称/角色）；重置密码后新密码登录成功、旧密码失效；分页关键字查询 | 通过 |
| 用户删除 | 删除用户后积分账户等关联数据级联清理（库内验证无孤儿记录） | 通过 |
| 课程学生 | 教师分页查学生（21 人）/候选列表/按 ID 添加/按用户名添加/重复添加幂等/添加非学生角色被拒/进度调整 55/非法进度 150 被拒/移除学生 | 通过 |
| 权限边界 | teacher2 访问/移除 teacher1 课程学生 → 403"只能管理自己创建的课程"；学生访问 → 403；admin 访问 teacher1/teacher2 课程学生 → 成功 | 通过 |

### 6.2 浏览器测试（全部通过）

管理端（http://localhost:5174）：

| 场景 | 结果 |
|------|------|
| admin 登录 → 系统设置：三项配置渲染、修改站点名称保存提示成功 | 通过 |
| admin → 用户管理：新增用户/编辑/重置密码弹窗字段完整 | 通过 |
| teacher1 登录 → 学生管理：课程下拉 → 学生列表含进度条 → 添加学生弹窗双模式切换 | 通过 |
| admin → 学生管理：提示条显示、下拉含其他教师课程、选择 teacher2"UI 设计基础"加载 14 名学生 | 通过 |

### 6.3 构建

Maven 重新打包 `ai-learning-platform-1.0.0.jar`（BUILD SUCCESS），从 backend 目录重启后端，课程封面等静态资源路径正常。

---

## 七、遗留说明

- `glm-4.7-flash` 免费模型高峰期限流（HTTP 429 / code 1305）为外部容量问题，客户端已做 2 次指数退避重试 + 友好提示；如需更高可用性可切换付费模型（仅需改 `AI_MODEL` 环境变量）；
- 建表 SQL 已合入 `backend/sql/init.sql`，存量环境需单独执行 `system_config` 建表语句（本环境已执行）。

---

## 八、本阶段新增/修改文件清单

**后端（backend）**

- 新增：`module/ops/entity/SystemConfig.java`、`module/ops/service/SystemConfigService.java`、`module/course/service/CourseStudentService.java`、`module/course/controller/CourseStudentController.java`
- 修改：`module/ops/service/UserManageService.java`（增删改查/重置密码/级联删除）、`module/ops/controller/AdminOpsController.java`（用户与设置接口）、`module/ai/service/ZhipuAiClient.java`（总开关校验）、`module/user/controller/AuthController.java`（注册开关）、`resources/application.yml`（密钥与模型）、`sql/init.sql`（system_config 表）

**管理端（ai-learning-admin）**

- 新增：`src/views/admin/SystemSettingsView.vue`、`src/views/course/CourseStudentsView.vue`
- 修改：`src/views/admin/UserManageView.vue`、`src/api/ops.js`、`src/router/index.js`、`src/layouts/MainLayout.vue`
