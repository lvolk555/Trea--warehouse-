# 阶段十技术报告：系统设置、用户管理扩展与课程学生管理

> 日期：2026-09-02
> 范围：AI 辅助在线学习平台（后端 + 管理端）
> 目标：接入 GLM-4.7-Flash 新模型与新密钥并增加 AI 总开关；补全管理端"系统设置"功能点（含 AI 密钥/模型/端点运行时热配置）；扩展用户管理（管理员对学生/教师/管理员的增删改查、重置密码，二级路由分类 + 手机端适配）；新增课程学生管理（教师管理自己课程的学生、管理员可管理任意教师课程的学生，学习详情查看小节完成明细）；完成全量回归测试后同步仓库。

---

## 一、任务背景与要点拆解

本阶段收到四项需求，拆解为如下开发要点：

| # | 要点 | 实现方式 |
|---|------|----------|
| 1 | AI 功能使用新密钥，模型切换为 `glm-4.7-flash`；系统设置可手动修改 AI 密钥/模型/端点 | 更新 `application.yml` 默认配置；`system_config` 表新增 `ai_api_key`/`ai_model`/`ai_base_url`，`ZhipuAiClient` 运行时动态读取（配置指纹缓存，改动即时生效无需重启） |
| 2 | 管理端系统设置功能点 | 新建 `system_config` 表 + `SystemConfigService` + 管理端设置页（站点名称 / AI 总开关 / 注册开关 / AI 模型接入配置） |
| 3 | 用户管理扩展：admin 对学生/教师增删改查、修改密码；二级路由分类 + 手机端适配 | `UserManageService` 增加 create/update/delete/resetPassword，删除时级联清理业务数据；管理端拆分为学生/教师/管理员三个三级路由页，共用 `UserList` 组件（桌面表格 + 移动卡片双布局） |
| 4 | 课程学生管理：教师查看/管理自己课程报名学生；admin 可管理任意教师课程的学生；查看学生小节完成明细 | 新建 `CourseStudentService` + `CourseStudentController`（按角色鉴权）；移除进度调整，新增学习详情接口与抽屉展示 |

---

## 二、AI 模型切换与总开关

### 2.1 配置更新

`backend/src/main/resources/application.yml`：

```yaml
spring:
  ai:
    openai:
      api-key: ${ZHIPU_API_KEY:7bcfccd6e77e416789ae78ae4ddcec23.7OSmeF0NkcaMOqZS}
      base-url: ${AI_BASE_URL:https://open.bigmodel.cn/api/paas/v4}
      chat:
        options:
          model: ${AI_MODEL:glm-4.7-flash}
```

密钥与模型均支持环境变量覆盖，代码中不硬编码。

### 2.2 AI 功能总开关

`ZhipuAiClient` 注入 `SystemConfigService`，所有 AI 调用（流式答疑 `chatStream` / 非流式出题批改 `chat`）统一经过 `checkAvailable()` 校验：

```java
private void checkAvailable() {
    if (!systemConfigService.isEnabled("ai_enabled")) {
        throw new BizException("AI 功能已被管理员关闭");
    }
    if (!isConfigured()) {
        throw new BizException("AI 服务未配置，请联系管理员");
    }
}
```

管理员在系统设置页关闭总开关后，学生端答疑、教师端出题/批改均**不发起任何模型调用**，直接返回降级提示。

### 2.3 运行时动态配置（系统设置热更新密钥/模型/端点）

`system_config` 表扩展三项 AI 接入配置，`ZhipuAiClient.resolveModel()` 在每次调用时读取生效配置，与缓存指纹（`apiKey|baseUrl|model` 拼接）比对，任一变化即重建模型客户端，**修改后即时生效、无需重启**：

```java
private OpenAiChatModel resolveModel() {
    String apiKey = effectiveApiKey();   // 系统设置优先，回退 yml 默认
    String baseUrl = systemConfigService.getValueOrDefault("ai_base_url", defaultBaseUrl);
    String model = systemConfigService.getValueOrDefault("ai_model", defaultModel);
    String fingerprint = apiKey + "|" + baseUrl + "|" + model;
    if (cachedModel == null || !fingerprint.equals(cachedFingerprint)) {
        // 重建 OpenAiApi + OpenAiChatModel 并刷新缓存
    }
    return cachedModel;
}
```

密钥安全处理：

- 回显：`SystemConfigService.list()` 对 `ai_api_key` 做**脱敏展示**（保留前 4 后 4 位，中间打码）；
- 保存：前端密钥输入框留空表示"不修改"；后端保存时空值直接跳过，避免脱敏值回写覆盖真实密钥。

出网代理兼容：模型客户端基于 Reactor Netty，启用 `proxyWithSystemProperties()`（仅当 JVM 设置了 `-Dhttps.proxyHost` 等参数时生效，直连环境不受影响），适配需要代理出网的部署环境。

### 2.4 限流与超时保护

- `429` 识别覆盖三层包装：原生 `RestClientResponseException`、WebClient 流式异常、Spring AI 包装的 `AiException`（消息形如 `429 - {...}`），任一命中均触发**最多 2 次指数退避重试**；
- 流式答疑超时 120s（免费模型高峰期首 token 可达 60s+）、非流式出题/批改超时 150s；
- 重试耗尽仍限流时提示"AI 服务限流，请稍后重试"。

### 2.5 实测结论

- 直连智谱 API 与经平台出题接口实测均成功（生成判断题 2 道、单选 2 道）；
- 学生端 AI 答疑流式问答端到端成功（45s 完整回复含代码示例）；
- 系统设置中修改模型为 `glm-4.7-flash` 后，后端日志确认配置热加载；
- `glm-4.7-flash` 为免费模型，高峰期存在间歇性 `429（code 1305 该模型当前访问量过大）`与响应变慢，已内置上述重试与超时保护，属外部服务容量问题，非集成缺陷。

---

## 三、管理端系统设置

### 3.1 数据库

新增 `system_config` 表（键值对 + 说明 + 更新时间，`config_key` 唯一）：

```sql
CREATE TABLE `system_config` (
  `id`           BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
  `config_key`   VARCHAR(50)  NOT NULL COMMENT '配置键',
  `config_value` VARCHAR(200) NOT NULL DEFAULT '' COMMENT '配置值',
  `remark`       VARCHAR(100) COMMENT '配置说明',
  `update_time`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE = InnoDB COMMENT = '系统设置表';
```

初始配置项：

| config_key | 说明 | 默认值 |
|-----------|------|--------|
| `site_name` | 站点名称 | AI 辅助在线学习平台 |
| `ai_enabled` | AI 功能总开关（1 开启 0 关闭） | 1 |
| `register_enabled` | 开放学生自主注册（1 开启 0 关闭） | 1 |
| `ai_api_key` | AI 服务 API 密钥（留空使用后端默认配置） | ''（空） |
| `ai_model` | AI 模型名称（如 glm-4.7-flash） | ''（空） |
| `ai_base_url` | AI 服务端点（OpenAI 兼容） | ''（空） |

### 3.2 后端

- `SystemConfigService.list()` / `save(Map)`：仅管理员可操作（`UserContext.checkRole(ROLE_ADMIN)`）；保存时校验 key 白名单，逐项 upsert；空值跳过（支持"留空不修改"语义）；
- `list()` 对 `ai_api_key` 脱敏回显（保留前 4 后 4 位）；`getValueOrDefault(key, default)` 供 AI 客户端读取（系统设置优先，回退 yml 默认值）；
- `isEnabled(key)`：供各业务读取开关（默认开启，仅显式为 `0` 时关闭）；
- 接口：`GET /admin/ops/settings`、`POST /admin/ops/settings`（批量保存）。

### 3.3 注册开关联动

`AuthController.register` 在注册前检查 `register_enabled`，关闭时返回"平台已关闭自主注册，请联系管理员开通账号"——关闭自主注册后，账号统一由管理员在"用户管理"中创建。

### 3.4 管理端页面

`SystemSettingsView.vue`（路由 `/admin/settings`，菜单"系统设置"）：

- **基础设置**：站点名称输入框 + 注册开关（带状态说明文案）；
- **AI 功能设置**：AI 总开关；**模型接入**区块——API 密钥（密码框，留空不修改，placeholder 显示当前脱敏密钥）、模型名称（自动完成，内置 glm-4.7-flash / glm-4.6-flash / glm-4.5-flash 等建议项，可自由输入）、服务端点 Base URL（OpenAI 兼容，留空用后端默认）；
- 保存后 AI 配置即时生效，无需重启后端。

---

## 四、用户管理扩展

### 4.1 后端接口

`AdminOpsController` 新增：

| 接口 | 说明 |
|------|------|
| `POST /admin/ops/users` | 新增用户（指定学生/教师/管理员角色 + 初始密码） |
| `PUT /admin/ops/users/{userId}` | 编辑用户（昵称/角色/状态） |
| `DELETE /admin/ops/users/{userId}` | 删除用户（级联清理业务数据） |
| `POST /admin/ops/users/{userId}/reset-password` | 重置密码 |

### 4.2 业务规则

- **新增**：用户名唯一校验；密码 BCrypt 加密；角色限定学生(1)/教师(2)/管理员(3)；初始积分账户同步创建；
- **编辑**：用户名不可修改；角色可调整；
- **重置密码**：新密码 BCrypt 加密后覆盖，旧密码立即失效；
- **删除**：不允许删除自己（`admin` 删除自己会导致平台无管理员）；事务内级联清理该用户的 AI 会话与消息、选课、学习记录、笔记、考试答题/考试记录/练习记录、积分账户/流水/签到/活动/优惠券、课程评论，最后删除用户本体，避免孤儿数据。

### 4.3 管理端页面（二级路由分类 + 手机端适配）

按"二级路由为分类、三级路由为功能界面"的结构改造：侧边栏"用户管理"为分组菜单，展开后三个子项分别对应三个三级路由页，共用 `UserList.vue` 组件（`role` prop 区分角色）：

| 路由 | 页面 | 角色 |
|------|------|------|
| `/admin/user/student` | 学生管理 | 学生（23 个账号） |
| `/admin/user/teacher` | 教师管理 | 教师 |
| `/admin/user/manager` | 管理员账号 | 管理员 |

`UserList.vue` 功能与适配：

- 工具栏：关键字搜索（用户名/昵称）+ 状态筛选 + 新增用户按钮；
- 行操作：编辑（弹窗回填，用户名不可改）、重置密码（弹窗输入新密码）、禁用/启用、删除（二次确认）；当前登录账号显示"当前账号"标识，不提供操作；
- **桌面端**（>768px）：表格布局，操作列固定右侧；
- **手机端**（≤768px）：卡片列表布局——头像 + 昵称/用户名 + 状态徽标 + 注册时间 + 块状操作按钮 + 简洁分页器；弹窗宽度自适应（92%）；
- 通过 `window.innerWidth` + `resize` 监听动态切换布局（onUnmounted 时移除监听）。

---

## 五、课程学生管理

### 5.1 权限模型

| 角色 | 可见课程 | 可执行操作 |
|------|---------|-----------|
| 教师 | 仅自己创建的课程 | 查看学生分页 / 添加学生 / 查看学习详情（小节完成明细）/ 移除学生 |
| 管理员 | 全部教师创建的课程 | 同教师（可跨教师操作任意课程） |
| 学生 | 无权限 | 接口直接 403 |

`checkCourseAccess()` 统一鉴权：教师访问他人课程返回 `403 只能管理自己创建的课程`。

### 5.2 后端接口

`CourseStudentController`（`/course/{courseId}/students`）：

| 接口 | 说明 |
|------|------|
| `GET ?page=&size=&keyword=` | 课程学生分页（含学生信息、账号状态、学习进度、选课时间），支持用户名/昵称关键字过滤 |
| `GET /candidates?keyword=` | 可添加学生候选（未选本课的启用状态学生，最多 50 条） |
| `POST` | 添加学生（按 `studentId` 或 `username`；仅学生角色；已在课幂等返回） |
| `GET /{studentId}/progress` | **学习详情**：该学生在本课程各章节下每个小节的完成状态（视频/图文类型、时长、完成标记、最近学习时间），按章节分组并含 x/y 小节统计 |
| `DELETE /{enrollmentId}` | 移除学生（删除选课记录，学习记录保留但不再计入课程） |

> 按需求调整：移除原"进度调整"接口（`PUT /{enrollmentId}`），改为教师/管理员查看学生小节完成明细的只读学习详情。

### 5.3 管理端页面

新建共用页面 `CourseStudentsView.vue`：

- **教师端**：菜单"教学管理 → 学生管理"（路由 `/teacher/course/students`），课程下拉仅显示自己的课程；
- **管理员端**：菜单"课程管理 → 学生管理"（路由 `/admin/course/students`），课程下拉显示全部课程，页面附权限说明提示条；
- 功能：课程选择 → 学生分页表格（头像/昵称/账号状态/进度条/选课时间）→ 添加学生弹窗（"从候选学生选择"支持搜索、"按用户名添加"双模式）→ **学习详情抽屉** → 移除二次确认；
- **学习详情抽屉**：按章节折叠面板展示——章节标题 + "x/y 小节"完成度标签；小节行含类型图标（视频/图文）、标题、时长、完成状态（绿色对勾 + 最近学习时间 / 灰色"未完成"）；手机端抽屉全屏（100% 宽）、桌面端 520px。

---

## 六、回归测试

### 6.1 接口测试（全部通过）

| 模块 | 用例 | 结果 |
|------|------|------|
| 系统设置 | 管理员查询配置（含 ai_api_key 脱敏回显、ai_model、ai_base_url）；保存模型为 glm-4.7-flash 后回读确认 | 通过 |
| AI 动态配置 | 保存模型后发起 AI 调用，后端日志确认"AI 模型配置已加载/刷新: model=glm-4.7-flash"（无需重启） | 通过 |
| AI 总开关 | 关闭后学生答疑与教师出题均被拒（"AI 功能已被管理员关闭"）；开启后恢复 | 通过 |
| AI 答疑 | 学生流式提问，SSE 返回完整回复（免费模型高峰期需重试，限流时提示"AI 服务限流"） | 通过 |
| AI 出题 | 教师出题接口生成完整题目（题干/选项/答案/解析） | 通过 |
| 用户管理 | 新增学生（建号成功）；编辑昵称；重置密码后新密码登录成功；禁用；删除后登录失败 | 通过 |
| 课程学生 | 候选学生列表/添加学生/列表确认/移除学生/重新添加（幂等） | 通过 |
| 学习详情 | 学生小节完成明细（章节分组、finished 标记、lastLearnTime） | 通过 |
| 权限边界 | teacher2 访问 teacher1 课程学生 → 403"只能管理自己创建的课程"；admin 访问任意课程学生及学习详情 → 成功 | 通过 |

### 6.2 浏览器测试（全部通过）

管理端（http://localhost:5174）：

| 场景 | 结果 |
|------|------|
| admin → 用户管理三个子路由页（/admin/user/student、/admin/user/teacher、/admin/user/manager）均正常渲染，列表与 CRUD 按钮齐全 | 通过 |
| admin → 系统设置：基础设置 + AI 功能设置（API 密钥/模型名称/Base URL 字段）渲染完整 | 通过 |
| admin → 课程学生管理：学习详情抽屉展示章节分组、小节完成/未完成标记、x/y 小节统计标签 | 通过 |
| 修复模板中直接访问 `window` 导致的 Vue 警告（改用 isMobile 响应式变量） | 通过 |

学生端（http://localhost:5173）：

| 场景 | 结果 |
|------|------|
| student1 登录 → AI 答疑提问 → 45s 收到完整流式回复（含代码示例），控制台无错误 | 通过 |

手机端适配说明：用户管理卡片布局与学习详情全屏抽屉通过代码审查 + 逻辑验证（isMobile 断点 768px、resize 监听），与项目内已验证的移动端页面采用同一实现模式；测试环境浏览器工具不支持窄视口模拟，未截图验证。

### 6.3 构建与运行

Maven 重新打包 `ai-learning-platform-1.0.0.jar`（BUILD SUCCESS），从 backend 目录重启后端，课程封面等静态资源路径正常。沙箱环境出网需代理，后端启动命令附带 JVM 代理参数（`-Dhttps.proxyHost` 等），AI 客户端已适配（见 2.3 节），生产直连环境无需该参数。

---

## 七、遗留说明

- `glm-4.7-flash` 免费模型高峰期限流（HTTP 429 / code 1305）为外部容量问题，客户端已做 2 次指数退避重试 + 友好提示；如需更高可用性可切换付费模型（仅需改 `AI_MODEL` 环境变量或在系统设置中修改模型名）；
- 建表 SQL 已合入 `backend/sql/init.sql`（含 ai_api_key/ai_model/ai_base_url 三项初始配置），存量环境需单独执行 `system_config` 建表与插入语句（本环境已执行）。

---

## 八、界面去 ID 化：统一展示名称

> 用户反馈：前端界面不应出现 ID 等技术标识，一律展示名称。

### 8.1 排查与修改

全量排查两端的 ID 暴露点，共四处：

| 位置 | 修改前 | 修改后 |
|------|--------|--------|
| 管理端用户管理 `UserList.vue` | 表格首列显示"ID" | 移除 ID 列（用户名/昵称/状态/注册时间/操作） |
| 管理端 AI 批改 `AiGradeView.vue` | 标签"学生 ID：7" | 标签"学生：陈晨"（后端 `pendingGrades` 新增 `studentName`，昵称优先回退用户名） |
| 管理端兑换记录 `ExchangeRecordView.vue` | 数字输入框"按学生 ID 筛选"（需先查 ID 才能筛） | 文本搜索框"按学生名称/用户名搜索"（后端 `adminPage` 改为 `keyword` 模糊匹配昵称/用户名） |
| 学生端积分中心 `PointsCenterView.vue` | 兑换记录列"课程 ID"显示 `#14` | 列"课程名称"显示"【测试】积分兑换课"（后端 `myExchanges` 返回 `CourseExchangeRecordVO.courseName`，课程已下架时显示提示文案） |

后端配套变更：

- `AiGenerateService.pendingGrades()`：批量查询学生表填充 `studentName`（避免 N+1，一次 `selectBatchIds`）；
- `ExchangeService.myExchanges()`：返回 VO 并填充 `courseName`；
- `ExchangeService.adminPage(page, size, keyword)`：入参由 `userId` 改为 `keyword`，先按昵称/用户名模糊匹配命中学生集合，再过滤兑换记录；无命中直接返回空页；
- 对应 Controller 签名同步调整。

### 8.2 附带修复：预览代理下中文查询参数被拒（400）的问题

**现象**：管理端兑换记录按"张"搜索时，请求返回 400 且无响应体；用 ASCII 关键字（stu01）正常。

**排查过程**（详见 6.4）：直连后端（8080）与经 Vite 代理（5174）的同等请求均 200；通过沙箱预览代理（16000，用户实际访问入口）的编码中文请求被拒；后端日志无对应请求记录，确认 400 由代理层产生。

**根因**：预览代理对 query string 会预先**解码一次**并拒绝含非 ASCII 字符的请求，然后再转发解码后的形式。axios 默认单层编码（`张` → `%E5%BC%A0`）在该代理下直接被拒。

**修复方案**（前后端配合，双通道兼容）：

1. **前端（管理端 + 学生端 `utils/request.js`）**：自定义 `paramsSerializer`，对参数值二次编码（`encodeURIComponent` 后将 `%` 再替换为 `%25`），使请求在代理处解码一次后剩余内容仍为纯 ASCII，可通过校验；
2. **后端（新增 `config/QueryParamDecodeFilter.java`）**：请求参数若仍含合法 `%XX` 序列且可解码为有效 UTF-8，则再解码一次。直连访问（Tomcat 已解码一层 + 过滤器再解一层）与经代理访问（代理解一层 + Tomcat 解一层，参数已还原）两种路径结果一致；
3. 过滤器跳过 multipart 请求，避免提前触发文件上传解析。

**兼容性验证**：

| 场景 | 结果 |
|------|------|
| 直连后端 + 单层编码（原有调用方式，如接口测试脚本） | 通过（过滤器不影响无 %XX 的参数） |
| 直连后端 + 双重编码（新前端行为） | 通过（过滤器二次解码） |
| 预览代理 + 双重编码（用户实际访问路径） | 通过（代理解码一层，Tomcat 解一层） |
| ASCII 关键字 | 通过（无编码参与，行为不变） |
| 文件上传（multipart） | 通过（过滤器跳过） |

已知极端边界：直连路径下搜索内容恰好包含 `%XX` 形态字面量的关键字会被多解一层（实际业务几乎不可能出现），可接受。

### 8.3 回归测试结果

| 用例 | 结果 |
|------|------|
| 用户管理中文搜索（张，单层/双重编码） | 均返回 2 条，一致 |
| 教师课程学生列表（21 人）| 通过 |
| 学习小节明细（2 章） | 通过 |
| 学生兑换记录 courseName | 【测试】积分兑换课 |
| 兑换记录 ASCII 搜索（9 条）/ 中文搜索（张同学） | 通过 |
| 浏览器端：兑换记录页搜索"张" | 无错误提示，正确显示张同学记录 |
| 浏览器端：用户管理无 ID 列、积分中心显示课程名称 | 通过 |

---

## 九、本阶段新增/修改文件清单

**后端（backend）**

- 新增：`module/ops/entity/SystemConfig.java`、`module/ops/service/SystemConfigService.java`（含 AI 配置项与脱敏）、`module/course/service/CourseStudentService.java`（含 progressDetail 学习明细）、`module/course/controller/CourseStudentController.java`、`config/QueryParamDecodeFilter.java`（查询参数容错解码，兼容代理预解码）
- 修改：`module/ops/service/UserManageService.java`（增删改查/重置密码/级联删除）、`module/ops/controller/AdminOpsController.java`（用户与设置接口）、`module/ai/service/ZhipuAiClient.java`（总开关校验 + 动态配置指纹缓存 + 代理兼容 + 429 重试识别 + 超时放宽）、`module/ai/service/AiGenerateService.java`（待批改列表填充 studentName）、`module/user/controller/AuthController.java`（注册开关）、`module/points/service/ExchangeService.java`（myExchanges 返回课程名称、adminPage 改按学生名称关键字筛选）、`module/points/controller/StudentPointsController.java`、`module/points/controller/AdminPointsController.java`、`resources/application.yml`（密钥与模型）、`sql/init.sql`（system_config 表及 AI 配置项）

**管理端（ai-learning-admin）**

- 新增：`src/views/admin/user/UserList.vue`（用户管理共用组件：桌面表格 + 移动卡片，无 ID 列）、`src/views/admin/user/UserStudentView.vue`、`src/views/admin/user/UserTeacherView.vue`、`src/views/admin/user/UserManagerAccountView.vue`
- 修改：`src/views/admin/SystemSettingsView.vue`（AI 模型接入配置区）、`src/views/course/CourseStudentsView.vue`（学习详情抽屉替代进度调整、移动端适配）、`src/views/teacher/AiGradeView.vue`（学生 ID → 学生名称）、`src/views/admin/ExchangeRecordView.vue`（按学生 ID 筛选 → 按学生名称搜索）、`src/utils/request.js`（参数二次编码序列化）、`src/api/ops.js`（学习明细接口、移除进度调整接口）、`src/router/index.js`（用户管理二级分组路由）、`src/layouts/MainLayout.vue`（用户管理分组菜单）
- 删除：`src/views/admin/UserManageView.vue`（由 UserList 组件三页替代）

**学生端（ai-learning-student）**

- 修改：`src/views/points/PointsCenterView.vue`（兑换记录课程 ID → 课程名称）、`src/utils/request.js`（参数二次编码序列化）
